eureka:
  instance:
    prefer-ip-address: true
    secure-port: ${server.port}
    secure-port-enabled: true
    non-secure-port-enabled: false
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}
  client:
    secure-port-enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@${spring.cloud.client.ip-address}:${server.port}/eureka/
      ## defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@10.69.97.21:8761/eureka/,http://${spring.security.user.name}:${spring.security.user.password}@10.69.97.22:8761/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 0
    enable-self-preservation: true
    eviction-interval-timer-in-ms: 5000
    response-cache-auto-expiration-in-seconds: 60
    renewal-percent-threshold: 0.85
    leaseRenewalIntervalInseconds: 10